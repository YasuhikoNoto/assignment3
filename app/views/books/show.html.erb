<div>
  <p><%= image_tag @book.get_image(100,100) %></p>
  <% if @book.user == current_user %>
    <%= link_to "Destroy", book_path(@book), method: :delete %>
  <% end%>
</div>

<p>Userモデルからimageを引用したいが、現在はBookのカラムにimageを追加している状態<br>
後で修正する</p>